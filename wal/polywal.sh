#!/bin/env sh

polybarwal $1 && neofetch && pywalfox update && set_discord_bg_link_wal && godot4_wal && okular_wal;
cp -f ~/.cache/wal/zelkBD.theme.css ~/.config/BetterDiscord/themes/

#set_firefox_tabliss_wal &&
